const CSV = ".csv,text/csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/vnd.ms-excel"
const IMG = ".jpg,.jpeg,.png,.gif,.bmp,.webp,image/jpeg,image/png,image/gif,image/bmp,image/webp"

export const FILE_TYPES_STRING = Object.freeze({
  CSV: CSV,
  XLSX: CSV,
  IMG: IMG,
})
