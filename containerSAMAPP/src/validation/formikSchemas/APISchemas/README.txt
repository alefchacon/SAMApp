The following are schemas that match those used by the API serializers 
when validating new model data. Update these whenever updating those
in the API.

The API stores these values in container-SAMUV-Server > principal > 
regexs.py. 

Examples of usage in API:  
   container-SAMUV-Server > user > serializers.py > AcademicSerializer 